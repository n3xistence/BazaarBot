import { ModalInteraction } from "./IModalInteraction";
import { add_task_winner } from "./add_task_winner";
import { card_50_pick } from "./card_50_pick";

export const Modals: Array<ModalInteraction> = [add_task_winner, card_50_pick];

import { describe, expect, test } from "@jest/globals";
import * as helper from "../src/ext/Helper";

const userID = "";

describe("init", () => {
  test("Initializing...", () => {
    expect(1).toBe(1);
  });
});
